// function testFunction(block) {
//     block.setAttribute('id', `bbbb`);
//     return block;
// }

// export {
//   testFunction,
// };

export default function testFunction(block) {
    block.setAttribute('id', `bbbb`);
    return block;
}